server:
  port: 8081
#  servlet:
#    context-path: /hotel


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/microservices
    username: root
    password: Shivam@219@
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: HOTEL-SERVICE
  config:
    import: optional:configserver:http://localhost:8085/actuator/default

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address:  true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8080/eureka





#    properties:
#      dialect:org.hibernate.dialect.MySQL8Dialect

